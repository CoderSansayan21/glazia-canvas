PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/glazia-canvas

# JWT Auth
JWT_SECRET=your_long_random_secret_here
JWT_EXPIRES_IN=7d

# CORS
CLIENT_URL=http://localhost:3000